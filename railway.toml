[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "uvicorn swarmz_server:app --host 0.0.0.0 --port $PORT --timeout-keep-alive 75"
healthcheckPath = "/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
